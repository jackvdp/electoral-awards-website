# Repo-wide ignores (the Next.js app has its own .gitignore in web/)

.DS_Store
.idea/

# safety net in case build artefacts ever land at the repo root
node_modules/
.next/
*.tsbuildinfo

docs
ICPS

projects/bsva
projects/

/emails
# Local symlink to web/.env.local (used by .claude/skills seed scripts)
.env.local

# edit-doc skill: local python venv
.claude/skills/edit-doc/.venv/
